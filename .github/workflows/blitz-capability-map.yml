name: ZEIBAEL Blitz Capability Map

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  capability-map-disabled-by-default:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Capability mapping is manual-only and not used in normal Blitz execution. ZEIBAEL compute lane uses MAX_BURST."
